# Anthropic API key — required for article reframing and leader lessons
ANTHROPIC_API_KEY=sk-ant-...

# GNews API key — required for news fetching (get one at https://gnews.io)
GNEWS_API_KEY=...

# Optional: secret token to protect the /api/refresh endpoint
REFRESH_SECRET=
